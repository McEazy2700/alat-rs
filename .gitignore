/target

# Secrets — never commit subscription keys / channel credentials
.env
.env.*
!.env.example

# Original Wema-published source map (input to the audit) — kept local;
# its corrected, schema-complete successor MY_API_MAP.md is committed.
api_map.md
